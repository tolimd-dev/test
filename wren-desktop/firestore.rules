rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All practice data — any authenticated practice member can read/write
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
